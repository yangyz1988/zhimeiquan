# 智媒圈 优化升级清单

> 审计日期：2026-07-04 | 基于全量代码审查

---

## 审计数据

| 维度 | 数据 |
|------|------|
| 后端代码总量 | ~16,700 行（104个.py文件 + 8,244行测试） |
| 前端代码总量 | ~10,000 行（15个页面 + 15个组件 + 前端API） |
| 关键模块总行数 | 4,209 行（browser/parser/scraper/analyzer/scheduler/competitor/notifier/hotspot/report/insights） |
| 测试代码 | 8,244 行 |
| Prisma Schema | 21 个数据模型 |
| API 路由 | 18 组后端 + 24 个前端代理 |

---

## 问题清单（按严重程度排序）

### 🔴 P0 — 必须立即修复

| # | 问题 | 位置 | 影响 |
|---|------|------|------|
| 1 | **`scheduler.py` 使用 `print()` 而非 `logger`** | `monitors/scheduler.py` — 15处 print | 生产环境日志不可控，无法接入日志系统 |
| 2 | **空 `except: pass` — 静默吞异常** | `services/agent.py` 第113/172行, `services/insights.py` 第178/241行 | 生产环境排查问题极困难 |
| 3 | **`main.py` /ready 端点有 TODO 注释** | `main.py` 第115行 — 数据库/Redis/LLM连通性检查未实现 | /ready 端点不能反映真实就绪状态 |
| 4 | **Schema 使用 SQLite 但 README 说 PostgreSQL** | `prisma/schema.prisma` — provider: "sqlite" | 生产环境性能/并发有隐患 |

### 🟡 P1 — 核心功能增强

| # | 问题 | 位置 | 影响 |
|---|------|------|------|
| 5 | **前端 tools 页面平台选择只有3个** | `tools/page.tsx` — `const platforms = ["抖音", "小红书", "B站"]` | 与宣传的13平台不符 |
| 6 | **generate-form 中平台列表只有6个** | `generate-form.tsx` — CONTENT_TYPES 中视频平台6个、图文平台5个 | 缺少快手、公众号、百度热搜等关键平台 |
| 7 | **日历页面使用大量 mock 数据** | `calendar/page.tsx` — `mockTitles`、`mockItem` 硬编码 | 用户看到的是假数据 |
| 8 | **前端 API 代理缺少统一错误处理** | 多个 API route — 返回不同格式的 fallback | 前端需分散处理各种错误格式 |
| 9 | **prompts.py 中平台特点硬编码** | `services/prompts.py` 第19-25行 — 写死5个平台的系统提示 | 新增平台需要改代码，且不如 `data/rules/` 文件的数据丰富 |
| 10 | **竞品监控前端页面缺少自动爬取按钮** | `monitor/page.tsx` — 有手动录入，无自动爬取 | 竞品内容仍需手动录入 |

### 🟢 P2 — 体验与完整性

| # | 问题 | 位置 | 影响 |
|---|------|------|------|
| 11 | **浏览器采集启动/停止无优雅处理** | `monitors/browser.py` — BrowserPool 没有 lifespan 集成 | 服务重启时可能有残留 Chromium 进程 |
| 12 | **无统一的分页/限流/缓存策略** | 分散在各 router | 接口一致性差 |
| 13 | **前端 reports 页面只有4个 mock 报告** | `reports/page.tsx` — MOCK_REPORTS 只有4个平台 | 其余9个平台的报告无法展示 |
| 14 | **operations 页面中 Agent 功能仅本地模拟** | `operations/page.tsx` — 点击生成走 setTimeout | 无法真正触发 Agent |
| 15 | **无移动端响应式适配** | 多个页面 — 表格/图表在小屏上不友好 | 创作者常在手机上使用 |
| 16 | **Fire Score 校准器依赖 numpy/scipy 但未被 API 暴露** | `analyzers/calibrator.py` — 有实现但无路由 | 用户无法使用校准功能 |
| 17 | **知识图谱模块无 API 暴露** | `services/knowledge_graph.py` — 有实现但无路由 | 功能不可用 |

### ⚪ P3 — 工程优化

| # | 问题 | 位置 | 影响 |
|---|------|------|------|
| 18 | **环境变量校验缺失** | 无集中校验 — 各模块自行 os.getenv | 配置错误时运行期才发现 |
| 19 | **无 CI/CD 流程的 e2e 测试** | `.github/workflows/ci.yml` 可能不完整 | 自动化验证不足 |
| 20 | **TypeScript strict 模式未开启** | `tsconfig.json` — 需确认 | 类型安全 |
| 21 | **Docker 健康检查不完善** | `docker-compose.prod.yml` — api 健康检查只验证8000端口可达 | 无法检测 Playwright/DeepSeek 是否就绪 |
| 22 | **content/ 知识库文件路径硬编码** | `content_loader.py` — 5个 Fire Score 方法论、3个人设 | 新增方法论需要改代码 |

---

## 优先级矩阵

```
                    影响大
                      │
         P0-1         │     P0-2
         P0-3         │     P0-4
    print→logger      │  静默异常
    /ready修复        │  SQLite→PG
                      │
   ───────────────────┼───────────────────→ 实现难度高
                      │
         P1-5         │     P1-6
         P1-7         │     P1-8
         平台列表      │     API错误处理
         假数据        │     prompts动态化
                      │
                      │     P1-9
                      │     竞品自动爬取
                      │
         P2-11        │     P2-12
         优雅关闭      │     分页/缓存
                      │
                      │     P2-13
                      │     报告覆盖
                    影响小
```

---

## 建议执行顺序

### 第一批（本周内）— 修复生产就绪问题

| 序号 | 任务 | 改动量 | 预计时间 |
|------|------|--------|---------|
| 1 | scheduler.py print → logger 替换 | 15行 | 10分钟 |
| 2 | insights.py/agent.py 空 except 改为具体异常类型 | 4处 | 15分钟 |
| 3 | main.py /ready 端点实现真实检查 | ~30行 | 30分钟 |
| 4 | Schema provider 注释 + 文档说明生产切换步骤 | 注释变更 | 5分钟 |

### 第二批（下周）— 核心功能增强

| 序号 | 任务 | 改动量 | 预计时间 |
|------|------|--------|---------|
| 5 | tools 页面平台列表扩展至 13 个 | ~10行 | 10分钟 |
| 6 | generate-form 平台列表扩展至 13 个 | ~20行 | 15分钟 |
| 7 | calendar 页面替换 mock 为真实 API | ~100行 | 2小时 |
| 8 | prompts.py 改为从 data/rules/ 动态注入平台信息 | ~50行 | 1小时 |
| 9 | 前端 API 统一错误处理中间件 | ~80行 | 1.5小时 |
| 10 | 竞品监控页面增加自动爬取按钮 | ~50行 | 1小时 |

### 第三批（本月）— 体验与完整性

| 序号 | 任务 | 改动量 | 预计时间 |
|------|------|--------|---------|
| 11 | BrowserPool lifespan 集成 + 优雅关闭 | ~40行 | 1小时 |
| 12 | API 统一分页/限流 | ~100行 | 2小时 |
| 13 | reports 页面补齐 13 个平台的 mock 数据 | ~50行 | 30分钟 |
| 14 | Fire Score 校准 API 暴露 | ~80行 | 1.5小时 |
| 15 | 知识图谱 API 暴露 | ~50行 | 1小时 |
| 16 | 移动端响应式基础适配 | ~200行 | 3小时 |

### 第四批（长期）— 工程基础

| 序号 | 任务 | 改动量 | 预计时间 |
|------|------|--------|---------|
| 17 | 环境变量集中校验模块 | ~100行 | 2小时 |
| 18 | CI/CD 完善 | 配置变更 | 1小时 |
| 19 | TypeScript strict 开启 + 类型修复 | 可能大量 | 视情况 |
| 20 | Docker 健康检查增强 | ~30行 | 30分钟 |
| 21 | content_loader 路径配置化 | ~30行 | 30分钟 |

---

## 总结

| 分类 | 数量 | 关键影响 |
|------|:---:|------|
| 🔴 P0 — 必须修复 | 4 | 生产就绪、日志可控、异常可见 |
| 🟡 P1 — 核心增强 | 6 | 平台覆盖一致性、数据真实性、API 质量 |
| 🟢 P2 — 体验完善 | 7 | 用户体验、功能可用性 |
| ⚪ P3 — 工程优化 | 5 | 长期可维护性 |

**总体评价**：项目在功能广度和技术深度方面已经非常扎实（~26K 行代码、13个平台、浏览器自动化采集、AI 评分闭环），当前主要问题是**"功能完备但细节粗糙"**——日志不规范、异常被吞、假数据混入真数据、前端平台列表不一致。这些问题不影响核心逻辑的演示，但会严重影响**生产环境的可靠性和用户信任**。
